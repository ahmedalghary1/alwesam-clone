# نظام استعادة كلمة المرور والبريد الإلكتروني - دليل الإعداد

## ✅ ما تم إنجازه

تم تنفيذ نظام كامل لاستعادة كلمة المرور وإرسال رسائل البريد الإلكتروني للمستخدمين:

### 1. إعدادات البريد الإلكتروني
- ✅ تم إضافة إعدادات SMTP في `settings.py`
- ✅ تم تكوين Gmail كخادم البريد الافتراضي

### 2. قاعدة البيانات
- ✅ تم إنشاء نموذج `PasswordResetCode` لتخزين رموز التحقق
- ✅ يتضمن: رمز 6 أرقام، تاريخ الانتهاء (15 دقيقة)، حالة الاستخدام

### 3. الواجهة الخلفية (Backend)
- ✅ تم إنشاء `email_utils.py` مع دوال إرسال البريد
  - `send_verification_code_email()` - إرسال رمز التحقق
  - `send_welcome_email()` - إرسال رسالة ترحيب
- ✅ تم تحديث `views.py` بإضافة:
  - `forgot_password_view()` - طلب استعادة كلمة المرور
  - `verify_code_view()` - التحقق من الرمز
  - `reset_password_view()` - إنشاء كلمة مرور جديدة
  - `resend_code_view()` - إعادة إرسال الرمز
- ✅ تم تحديث `register_view()` لإرسال رسالة ترحيب
- ✅ تم إضافة المسارات الجديدة في `urls.py`

### 4. الواجهة الأمامية (Frontend)
- ✅ تم إنشاء `forgot_password.html` - صفحة طلب استعادة كلمة المرور
- ✅ تم إنشاء `verify_code.html` - صفحة إدخال رمز التحقق (6 أرقام)
- ✅ تم إنشاء `reset_password.html` - صفحة إنشاء كلمة مرور جديدة
- ✅ تم تحديث رابط "نسيت كلمة المرور" في صفحة الدخول

---

## 🔧 خطوات الإعداد المطلوبة

### الخطوة 1️⃣: تفعيل البيئة الافتراضية وتشغيل Migrations

```bash
# انتقل إلى مجلد المشروع
cd /home/algohary/projects/alwesam-store/src

# فعّل البيئة الافتراضية (إذا كانت موجودة)
# source venv/bin/activate  # أو اسم البيئة الافتراضية لديك

# أنشئ الـ migrations
python3 manage.py makemigrations accounts

# طبق الـ migrations
python3 manage.py migrate
```

### الخطوة 2️⃣: إعداد البريد الإلكتروني

افتح ملف `project/settings.py` وحدث إعدادات البريد الإلكتروني:

#### أ) استخدام Gmail (للإنتاج):

```python
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = 'your-email@gmail.com'  # بريدك الإلكتروني
EMAIL_HOST_PASSWORD = 'your-app-password'  # كلمة مرور التطبيق
DEFAULT_FROM_EMAIL = 'متجر الأدوات الكهربائية <your-email@gmail.com>'
```

**ملاحظة مهمة**: لاستخدام Gmail، يجب:
1. تفعيل "التحقق بخطوتين" على حساب Gmail
2. إنشاء "كلمة مرور للتطبيق" (App Password):
   - اذهب إلى: https://myaccount.google.com/apppasswords
   - أنشئ كلمة مرور جديدة للتطبيق
   - استخدمها في `EMAIL_HOST_PASSWORD`

#### ب) للتطوير والاختبار (طباعة الرسائل في Console):

```python
EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'
```

هذا الخيار سيطبع رسائل البريد الإلكتروني في الـ console بدلاً من إرسالها فعلياً، مفيد للاختبار.

### الخطوة 3️⃣: تشغيل السيرفر واختبار النظام

```bash
python3 manage.py runserver
```

---

## 🧪 اختبار النظام

### 1. اختبار استعادة كلمة المرور:

1. اذهب إلى صفحة تسجيل الدخول: `http://localhost:8000/accounts/login/`
2. اضغط على "نسيت كلمة المرور؟"
3. أدخل بريدك الإلكتروني
4. تحقق من رسالة البريد الإلكتروني (أو console إذا كنت تستخدم console backend)
5. أدخل رمز التحقق المكون من 6 أرقام
6. أدخل كلمة المرور الجديدة
7. سجل الدخول بكلمة المرور الجديدة

### 2. اختبار رسالة الترحيب:

1. أنشئ حساب جديد: `http://localhost:8000/accounts/register/`
2. املأ البيانات وأكمل التسجيل
3. تحقق من البريد الإلكتروني لرسالة الترحيب

---

## 📁 الملفات المعدلة/المنشأة

### ملفات معدلة:
- ✅ `project/settings.py` - إضافة إعدادات البريد الإلكتروني
- ✅ `accounts/models.py` - إضافة نموذج PasswordResetCode
- ✅ `accounts/views.py` - إضافة views استعادة كلمة المرور
- ✅ `accounts/urls.py` - إضافة المسارات الجديدة
- ✅ `accounts/templates/accounts/login_page.html` - تحديث رابط نسيت كلمة المرور

### ملفات جديدة:
- ✅ `accounts/email_utils.py` - دوال إرسال البريد الإلكتروني
- ✅ `accounts/templates/accounts/forgot_password.html`
- ✅ `accounts/templates/accounts/verify_code.html`
- ✅ `accounts/templates/accounts/reset_password.html`

---

## 🎨 التصميم

جميع الصفحات الجديدة تتبع نفس الهوية البصرية للمشروع:
- ✅ الألوان: أسود (#000000)، أصفر ذهبي (#FFD700)، أبيض (#FFFFFF)
- ✅ الخط: Cairo (من Google Fonts)
- ✅ الاتجاه: RTL (من اليمين إلى اليسار)
- ✅ Bootstrap 5 RTL
- ✅ Font Awesome Icons
- ✅ تصميم متجاوب (responsive)

---

## ⚙️ الميزات

### استعادة كلمة المرور:
- ✅ رمز تحقق مكون من 6 أرقام
- ✅ صلاحية الرمز 15 دقيقة
- ✅ إمكانية إعادة إرسال الرمز
- ✅ مؤقت لعرض الوقت المتبقي
- ✅ إدخال تفاعلي للرمز (auto-focus على الحقل التالي)
- ✅ مؤشر قوة كلمة المرور
- ✅ نصائح أمنية لكلمة المرور

### رسائل البريد الإلكتروني:
- ✅ تصميم HTML جميل ومتطابق مع هوية المتجر
- ✅ رسالة ترحيب عند إنشاء حساب جديد
- ✅ رسالة رمز التحقق مع تعليمات واضحة

---

## 🔒 الأمان

- ✅ رموز التحقق تنتهي صلاحيتها بعد 15 دقيقة
- ✅ كل رمز يُستخدم مرة واحدة فقط
- ✅ لا يتم الكشف عما إذا كان البريد موجوداً أم لا (منع تعداد المستخدمين)
- ✅ التحقق من قوة كلمة المرور (8 أحرف على الأقل)
- ✅ كلمات المرور يتم تشفيرها باستخدام Django's password hashing

---

## 📝 ملاحظات

1. **بيانات الاعتماد**: لا تنس تحديث بيانات البريد الإلكتروني في `settings.py`
2. **للإنتاج**: استخدم متغيرات بيئة (environment variables) لتخزين بيانات البريد الإلكتروني بدلاً من كتابتها مباشرة
3. **للتطوير**: يمكنك استخدام `console backend` لاختبار النظام دون إرسال رسائل فعلية
4. **الـ Migrations**: يجب تشغيل migrations قبل استخدام النظام

---

## 🎯 الخطوات التالية (اختياري)

- [ ] إضافة اختبارات وحدة (Unit Tests)
- [ ] إضافة rate limiting لمنع إرسال طلبات متعددة
- [ ] إضافة CAPTCHA لمنع الهجمات الآلية
- [ ] تخصيص قوالب البريد الإلكتروني أكثر
- [ ] إضافة إحصائيات لرموز التحقق المرسلة
